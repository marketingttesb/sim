# Project Context

> Auto-generated by opencode. Update this as the project grows.

## Stack
<!-- e.g. Node.js 20, Hono, TypeScript, React, etc. -->

## Structure
```
D:\Dev\sim\
```

## Key Commands
- `npm run setup` — full environment install (npm prefix, MCP memory, skills, config, junction)
- `npm install` — auto-runs setup via postinstall hook

## Git
```
Belum ada repo.
```

## MCP
- Memory server: `D:\Apps\OpenCode\mcp-memory\server.mjs`
  - Type: `local` (NOT `stdio` — schema hanya terima `local`/`remote`)
  - Config di `C:\Users\User\.config\opencode\opencode.jsonc`
  - Tools: `read_memory`, `update_memory`, `list_memories`
- Permission: external directory `D:\Apps\OpenCode\memory\**` dah di-allow

## Notes
- Project masih kosong. Tunggu setup dari user.

## Memory System
- Memory files disimpan di `D:\Apps\OpenCode\memory\` (bukan dalam project)
  - `general/` — shared preferences, global session log
  - `projects/{nama_folder}/` — per-project context, log, decisions
- MCP server `memory` berjalan dari `D:\Apps\OpenCode\mcp-memory\server.mjs`
- Sidebar MCP section akan tunjuk memory files secara langsung
- Update AGENTS.md bila ada perubahan struktur, dependencies, atau keputusan penting.

## Auto Memory (CRITICAL)
- **Setiap kali sesi baru**, aku MESTI baca semua fail memory dari `D:\Apps\OpenCode\memory\` SEBELUM respond mesej pertama user.
- User TAK perlu cakap "ingat balik" atau "baca memory" — aku buat automatik.
- Auto-update memory bila ada info baru (keputusan, dependency, perubahan).
- Memory structure: `general/` (shared) + `projects/{nama_folder}/` (per-project)
